# Multi-Source Candidate Data Transformer — dependencies
# Install: pip install -r requirements.txt
# Then:    python -m spacy download en_core_web_sm

# ── GitHub API ────────────────────────────────────────────────────────────────
PyGithub>=2.1.1

# ── Resume parsing (requires spaCy + en_core_web_sm model) ───────────────────
pyresparser>=1.0.6
# PDF text extraction (pyresparser dependency — explicit for clarity)
pdfminer.six>=20221105
# DOCX support
docx2txt>=0.8

# ── NLP (required by pyresparser) ────────────────────────────────────────────
spacy>=3.7.0
# After installing spaCy, run: python -m spacy download en_core_web_sm

# ── Phone normalization ───────────────────────────────────────────────────────
phonenumbers>=8.13.0

# ── Country code normalization ────────────────────────────────────────────────
pycountry>=23.12.11

# ── Date parsing ─────────────────────────────────────────────────────────────
python-dateutil>=2.9.0

# ── Schema validation & serialization ────────────────────────────────────────
pydantic[email]>=2.5.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
